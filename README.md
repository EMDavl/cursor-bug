To start the app you have to have redis cluster somewhere.

1. Update host and port parameters in Example.java file
2. Execute `mvn package` in the project root folder
3. Start app with `java -jar cursor-bug-1.0-jar-with-dependencies.jar`
